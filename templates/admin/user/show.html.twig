{% extends 'base.html.twig' %}

{% block body %}
    <section class="first-section-wm">
        <table class="table table-hover white-text">
            <thead class="black">
            <tr>
                <th scope="col">id</th>
                <th scope="col">Email</th>
                <th scope="col">Role</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <th scope="row">{{ user.id }}</th>
                <td>{{ user.email }}</td>
                <td>{{ user.roles | json_encode() }}</td>
            </tr>
            </tbody>
        </table>
    </section>
{% endblock %}